<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.jobsdev">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon_jobsdev_purple_round"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon_jobsdev_purple_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:targetApi="m">
        <activity android:name=".onboard.OnBoardLoginActivity"></activity>
        <activity android:name=".maincontent.editprofile.EditAccountCompanyActivity" />
        <activity android:name=".maincontent.editprofile.EditAccountEngineerActivity" />
        <activity android:name=".maincontent.webview.GitHubWebViewActivity" />
        <activity android:name=".onboard.OnBoardRegisterActivity" />
        <activity android:name=".onboard.OnBoardRegLogActivity" />
        <activity android:name=".maincontent.MainContentActivity" />
        <activity android:name=".reset_password.ResetPasswordInputNewPasswordActivity" />
        <activity android:name=".reset_password.ResetPasswordSendEmailActivity" />
        <activity android:name=".register.RegisterCompanyActivity" />
        <activity android:name=".register.RegisterEngineerActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>